# ship orchestrator artifacts
.ship.lock
.ship-status.json
.ship-result.json
.ship-trace.jsonl
ESCALATION.md
graph.json
graph.html
GRAPH_REPORT.md
graphify-out/
# machine-specific launcher (absolute path) — created by ship-init, not shared
ship-app.cmd
# secrets live in their tools (fcc admin UI / codex login), never the repo
.env
.env.local

# ship-app persistent memory
.ship-app.json

# python bytecode
__pycache__/
*.pyc
